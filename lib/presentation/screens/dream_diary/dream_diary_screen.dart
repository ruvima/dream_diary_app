import 'package:flutter/material.dart';

class DreamDiary extends StatelessWidget {
  const DreamDiary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('DreamDiary')),
    );
  }
}
